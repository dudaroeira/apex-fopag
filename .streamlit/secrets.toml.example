# Copie este arquivo para .streamlit/secrets.toml em DESENVOLVIMENTO LOCAL,
# ou cole os valores em Streamlit Cloud → Settings → Secrets quando hospedar.
# NUNCA comite o secrets.toml de verdade (ele está no .gitignore).

SUPABASE_URL = "https://xxxxxxxxxxxx.supabase.co"
SUPABASE_KEY = "eyJhbGc..."   # use service_role key — o app é privado

# Lista de e-mails que veem o painel completo (sócios, diretor RH).
# Streamlit Cloud também permite controlar isso via Settings → Sharing.
ADMIN_EMAILS = ["dudaroeira@gmail.com"]
